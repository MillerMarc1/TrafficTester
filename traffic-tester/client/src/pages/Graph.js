import React from "react";
import { Box, Stack, Typography, TextField, Button } from "@mui/material";

const Graph = () => {
  return (
    <Box>
      <Stack direction={"row"}>
        <Stack style={{ color: "#804F3B" }} variant="h1">
          <Typography>Graph</Typography>
        </Stack>
      </Stack>
    </Box>
  );
};

export default Graph;
